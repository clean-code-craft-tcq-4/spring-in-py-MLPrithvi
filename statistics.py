def calculateStats(numbers):
  if numbers == 0:
    avgVal = "nan"
    minVal = "nan"
    maxVal = "nan"
  return avgVal,minVal,maxVal
#   if numbers != 0:
#     l = len(numbers)
#     avgVal = sum(numbers) / l
#     maxVal = max(numbers)
#     minVal = min(numbers)
